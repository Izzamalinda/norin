<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Generate QR Code Meja</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    .form-container {
      background: linear-gradient(145deg, #ffffff, #f9fafb);
      border: 1px solid #e5e7eb;
    }

    .form-container:hover {
      border-color: #f97316;
      box-shadow: 0 16px 32px rgba(249, 115, 22, 0.15);
    }

    .form-input {
      border: 2px solid #e5e7eb;
      transition: all 0.3s ease;
      border-radius: 10px;
      padding: 12px 16px;
      font-size: 16px;
    }

    .form-input:focus {
      border-color: #f97316;
      box-shadow: 0 0 0 3px rgba(249, 115, 22, 0.1);
      outline: none;
    }

    .btn-generate {
      background: linear-gradient(135deg, #f97316, #ea580c);
      transition: all 0.3s ease;
      border-radius: 10px;
      font-weight: 600;
      padding: 12px 20px;
    }

    .btn-generate:hover {
      background: linear-gradient(135deg, #ea580c, #dc2626);
      transform: translateY(-2px);
      box-shadow: 0 8px 16px rgba(249, 115, 22, 0.4);
    }

    .btn-generate:active {
      transform: translateY(0);
    }

    .qr-result {
      animation: slideIn 0.5s ease-out;
      background: linear-gradient(145deg, #ffffff, #f9fafb);
      border: 1px solid #e5e7eb;
      border-radius: 16px;
      padding: 32px;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .qr-image {
      transition: all 0.3s ease;
      border: 3px solid #e5e7eb;
      background: white;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    }

    .qr-image:hover {
      border-color: #f97316;
      transform: scale(1.02);
      box-shadow: 0 12px 24px rgba(249, 115, 22, 0.25);
    }

    .success-message {
      background: linear-gradient(135deg, #d4fc7c, #a3e635);
      border: 1px solid #84cc16;
      border-radius: 10px;
      padding: 12px 16px;
      color: #3f6212;
      font-weight: 600;
      animation: slideIn 0.3s ease-out;
      display: flex;
      align-items: center;
      space: 8px;
    }

    .success-icon {
      margin-right: 8px;
      font-size: 18px;
    }

    .link-back {
      color: #f97316;
      transition: all 0.3s ease;
      font-weight: 600;
      padding: 8px 12px;
      border-radius: 8px;
    }

    .link-back:hover {
      background: rgba(249, 115, 22, 0.1);
      padding-left: 16px;
    }

    .form-label {
      color: #1f2937;
      font-weight: 600;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      space: 4px;
    }

    .label-icon {
      color: #f97316;
      margin-right: 6px;
    }

    .section-divider {
      height: 1px;
      background: linear-gradient(90deg, transparent, #e5e7eb, transparent);
      margin: 24px 0;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-50 via-orange-50 to-red-50 min-h-screen flex">

  <!-- Sidebar -->
  <%- include('partials/sidebarAdmin') %>

  <!-- Main Content -->
  <main class="flex-1 ml-64 p-10">
    <div class="flex flex-col items-center justify-center min-h-screen">
      
      <!-- Header -->
      <div class="text-center mb-10">
        <div class="flex items-center justify-center space-x-4 mb-4">
          <div class="w-14 h-14 bg-gradient-to-br from-orange-400 to-red-600 rounded-2xl flex items-center justify-center shadow-lg">
            <i class="fas fa-qrcode text-white text-2xl"></i>
          </div>
          <h1 class="text-3xl font-bold text-gray-800">Generate QR Code Meja</h1>
        </div>
        <p class="text-gray-600 text-sm">Buat QR code baru untuk meja restoran Anda</p>
      </div>

      <!-- Form Container -->
      <div class="form-container p-10 rounded-3xl shadow-2xl w-full max-w-md">
        
        <!-- Success Message -->
        <% if (message) { %>
          <div class="success-message mb-6">
            <i class="fas fa-check-circle success-icon"></i>
            <span><%= message %></span>
          </div>
        <% } %>

        <!-- Form -->
        <form action="/meja/generate" method="POST" class="space-y-6">
          
          <!-- Input Group -->
          <div>
            <label class="form-label">
              <i class="fas fa-chair label-icon"></i>
              <span>Nomor Meja</span>
            </label>
            <input name="no_meja" 
                   type="number" 
                   placeholder="Contoh: 1, 2, 3..." 
                   class="form-input w-full text-gray-700 placeholder-gray-400"
                   min="1"
                   required />
          </div>

          <!-- Submit Button -->
          <button type="submit"
                  class="btn-generate w-full text-white flex items-center justify-center space-x-2">
            <i class="fas fa-qrcode text-lg"></i>
            <span>Generate QR Code</span>
          </button>
        </form>

        <!-- Divider -->
        <div class="section-divider"></div>

        <!-- QR Code Result -->
        <% if (qr) { %>
          <div class="qr-result">
            <p class="text-center text-gray-800 font-semibold mb-4">
              <i class="fas fa-check-circle text-green-500 mr-2"></i>
              QR Code Meja Berhasil Dibuat!
            </p>
            <div class="flex justify-center mb-6">
              <img src="<%= qr %>" 
                   alt="QR Meja" 
                   class="qr-image w-56 h-56" />
            </div>
            <p class="text-center text-gray-600 text-sm">
              <i class="fas fa-mobile-alt text-orange-500 mr-2"></i>
              Scan QR code di atas untuk melihat menu
            </p>
          </div>
        <% } %>

        <!-- Back Link -->
        <div class="text-center mt-8">
          <a href="/meja/list" 
             class="link-back inline-flex items-center justify-center space-x-2">
            <i class="fas fa-arrow-left"></i>
            <span>Kembali ke Daftar Meja</span>
          </a>
        </div>
      </div>

    </div>
  </main>

</body>
</html>