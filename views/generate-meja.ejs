<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Generate QR Code Meja</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    .main-content {
      margin-left: 256px;
    }

    @media (max-width: 1024px) {
      .main-content {
        margin-left: 0;
      }
    }

    .page-wrapper {
      min-h-screen;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 32px 16px;
    }

    .form-container {
      background: linear-gradient(145deg, #ffffff, #f9fafb);
      border: 1.5px solid #e5e7eb;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
      position: relative;
      overflow: hidden;
    }

    .form-container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, #f97316, #ea580c, #dc2626);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.3s ease;
    }

    .form-container:hover {
      border-color: #f97316;
      box-shadow: 0 12px 32px rgba(249, 115, 22, 0.15);
      transform: translateY(-4px);
    }

    .form-container:hover::before {
      transform: scaleX(1);
    }

    .form-input {
      border: 2px solid #e5e7eb;
      transition: all 0.3s ease;
      border-radius: 12px;
      padding: 14px 18px;
      font-size: 16px;
      background: white;
    }

    .form-input:hover {
      border-color: #f0f0f0;
    }

    .form-input:focus {
      border-color: #f97316;
      box-shadow: 0 0 0 4px rgba(249, 115, 22, 0.1);
      outline: none;
      background: linear-gradient(to right, #ffffff, #fafaf9);
    }

    .btn-generate {
      background: linear-gradient(135deg, #f97316, #ea580c);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      border-radius: 12px;
      font-weight: 700;
      padding: 14px 24px;
      box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
      border: none;
      cursor: pointer;
    }

    .btn-generate:hover {
      background: linear-gradient(135deg, #ea580c, #dc2626);
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(249, 115, 22, 0.4);
    }

    .btn-generate:active {
      transform: translateY(-1px);
    }

    .qr-result {
      animation: slideInUp 0.6s cubic-bezier(0.4, 0, 0.2, 1);
      background: linear-gradient(145deg, #ffffff, #f9fafb);
      border: 1.5px solid #e5e7eb;
      border-radius: 20px;
      padding: 40px;
      margin-top: 32px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    }

    @keyframes slideInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .qr-image {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      border: 3px solid #e5e7eb;
      background: white;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .qr-image:hover {
      border-color: #f97316;
      transform: scale(1.03);
      box-shadow: 0 12px 28px rgba(249, 115, 22, 0.25);
    }

    .success-message {
      background: linear-gradient(135deg, #d4fc7c, #a3e635);
      border: 1.5px solid #84cc16;
      border-radius: 12px;
      padding: 14px 18px;
      color: #3f6212;
      font-weight: 600;
      animation: slideInDown 0.4s ease-out;
      display: flex;
      align-items: center;
      gap: 10px;
      box-shadow: 0 4px 12px rgba(132, 204, 22, 0.2);
    }

    @keyframes slideInDown {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .success-icon {
      font-size: 20px;
      flex-shrink: 0;
    }

    .link-back {
      color: #f97316;
      transition: all 0.3s ease;
      font-weight: 600;
      padding: 10px 16px;
      border-radius: 10px;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
    }

    .link-back:hover {
      background: linear-gradient(135deg, rgba(249, 115, 22, 0.1), rgba(249, 115, 22, 0.05));
      transform: translateX(-4px);
    }

    .form-label {
      color: #1f2937;
      font-weight: 700;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 15px;
      letter-spacing: 0.3px;
    }

    .label-icon {
      color: #f97316;
      font-size: 18px;
      width: 24px;
      height: 24px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .section-divider {
      height: 1px;
      background: linear-gradient(90deg, transparent, #e5e7eb, transparent);
      margin: 28px 0;
    }

    .header-section {
      text-align: center;
      margin-bottom: 32px;
    }

    .header-icon {
      width: 60px;
      height: 60px;
      margin: 0 auto 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #f97316, #ea580c);
      border-radius: 20px;
      box-shadow: 0 8px 20px rgba(249, 115, 22, 0.3);
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-8px); }
    }

    .header-icon i {
      color: white;
      font-size: 28px;
    }

    .header-title {
      font-size: 32px;
      font-weight: 800;
      color: #1f2937;
      margin-bottom: 12px;
      letter-spacing: -0.5px;
    }

    .header-subtitle {
      color: #6b7280;
      font-size: 15px;
      font-weight: 500;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .success-check {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      margin-bottom: 20px;
      font-weight: 600;
      color: #1f2937;
      font-size: 18px;
    }

    .success-check i {
      color: #10b981;
      font-size: 24px;
    }

    .qr-info {
      text-align: center;
      color: #6b7280;
      font-size: 14px;
      margin-top: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      font-weight: 500;
    }

    .qr-info i {
      color: #f97316;
      font-size: 18px;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-50 via-orange-50 to-red-50 min-h-screen">
  <%- include('partials/sidebarAdmin') %>

  <main class="main-content">
    <div class="page-wrapper">
      <div class="w-full max-w-md">
        
        <!-- Header Section -->
        <div class="header-section">
          <div class="header-icon">
            <i class="fas fa-qrcode"></i>
          </div>
          <h1 class="header-title">Generate QR Code</h1>
          <p class="header-subtitle">Buat QR code untuk meja restoran Anda</p>
        </div>

        <!-- Form Container -->
        <div class="form-container p-8 rounded-3xl">
          
          <!-- Success Message -->
          <% if (message) { %>
            <div class="success-message mb-6">
              <i class="fas fa-check-circle success-icon"></i>
              <span><%= message %></span>
            </div>
          <% } %>

          <!-- Form -->
          <form action="/meja/generate" method="POST" class="space-y-6">
            
            <!-- Input Group -->
            <div class="form-group">
              <label class="form-label">
                <div class="label-icon">
                  <i class="fas fa-chair"></i>
                </div>
                <span>Nomor Meja</span>
              </label>
              <input name="no_meja" 
                     type="number" 
                     placeholder="Masukkan nomor meja..." 
                     class="form-input text-gray-700 placeholder-gray-400"
                     min="0"
                     required />
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn-generate w-full text-white flex items-center justify-center gap-2">
              <i class="fas fa-qrcode"></i>
              <span>Generate QR Code</span>
            </button>
          </form>

          <!-- Divider -->
          <div class="section-divider"></div>

          <!-- QR Code Result -->
          <% if (qr) { %>
            <div class="qr-result">
              <div class="success-check">
                <i class="fas fa-check-circle"></i>
                <span>QR Code Siap!</span>
              </div>

              <div class="flex justify-center mb-6">
                <img src="<%= qr %>" 
                     alt="QR Meja" 
                     class="qr-image w-56 h-56" />
              </div>

              <div class="qr-info">
                <i class="fas fa-mobile-alt"></i>
                <span>Scan untuk melihat menu</span>
              </div>
            </div>
          <% } %>

          <!-- Back Link -->
          <div class="text-center mt-8">
            <a href="/meja/list" class="link-back">
              <i class="fas fa-arrow-left"></i>
              <span>Kembali ke Daftar Meja</span>
            </a>
          </div>
        </div>

      </div>
    </div>
  </main>

</body>
</html>