<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8" />
  <title>
    <%= title %>
  </title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    .card-hover {
      transition: all 0.3s ease;
    }

    .card-hover:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    }

    .stat-card {
      background: #fff;
      border: 1px solid #e5e7eb;
    }

    .chart-container {
      background: #fff;
      border: 1px solid #e5e7eb;
    }

    .status-badge {
      @apply px-3 py-1 rounded-full text-xs font-medium;
    }

    .status-pending {
      @apply bg-yellow-100 text-yellow-800;
    }

    .status-completed {
      @apply bg-green-100 text-green-800;
    }

    .status-cancelled {
      @apply bg-red-100 text-red-800;
    }

    .status-processing {
      @apply bg-blue-100 text-blue-800;
    }

    .main-content {
      margin-left: 256px;
    }

    @media (max-width: 1024px) {
      .main-content {
        margin-left: 0;
      }
    }

    .dashboard-header {
      position: sticky;
      top: 0;
      z-index: 30;
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(8px);
      transition: box-shadow 0.3s ease;
    }

    .dashboard-header.scrolled {
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .custom-scrollbar::-webkit-scrollbar {
      height: 8px;
    }

    .custom-scrollbar::-webkit-scrollbar-thumb {
      background: #cbd5e1;
      border-radius: 4px;
    }

    .custom-scrollbar::-webkit-scrollbar-thumb:hover {
      background: #94a3b8;
    }
  </style>
</head>

<body class="bg-gray-50">
  <!-- Sidebar -->
  <%- include('partials/sidebarAdmin') %>

    <!-- Main Content -->
    <div class="main-content min-h-screen">
      <!-- Header -->
      <header class="dashboard-header border-b border-gray-200">
        <div class="px-6 py-6 flex items-center space-x-4">
          <div class="w-10 h-10 bg-orange-500 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
          </div>
          <div>
            <h1 class="text-2xl font-bold text-gray-800">Dashboard Admin</h1>
            <p class="text-gray-600 text-sm">Pantau aktivitas dan performa bisnis Anda</p>
          </div>
        </div>
      </header>

      <!-- Content -->
      <main class="px-6 py-8">

        <!-- Stats -->
        <section class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
          <div class="stat-card card-hover p-6 rounded-xl shadow-sm">
            <p class="text-sm font-medium text-gray-500 mb-2">Revenue Hari Ini</p>
            <p id="revenueToday" class="text-2xl font-bold text-gray-900">Rp 0</p>
          </div>

          <div class="stat-card card-hover p-6 rounded-xl shadow-sm">
            <p class="text-sm font-medium text-gray-500 mb-2">Total Pesanan</p>
            <p id="totalPesanan" class="text-2xl font-bold text-gray-900">0</p>
          </div>

          <div class="stat-card card-hover p-6 rounded-xl shadow-sm">
            <p class="text-sm font-medium text-gray-500 mb-2">Menu Aktif</p>
            <p id="menuAktif" class="text-2xl font-bold text-gray-900">0</p>
          </div>

          <div class="stat-card card-hover p-6 rounded-xl shadow-sm">
            <p class="text-sm font-medium text-gray-500 mb-2">Meja Terisi</p>
            <p id="customerAktif" class="text-2xl font-bold text-gray-900">0</p>
          </div>
        </section>

        <!-- Penjualan & Kategori -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
          <!-- Penjualan -->
          <div class="chart-container card-hover p-6 rounded-xl shadow-sm">
            <h2 class="text-xl font-bold text-gray-800 mb-4">Penjualan 7 Hari Terakhir</h2>
            <div class="overflow-x-auto custom-scrollbar">
              <table id="salesTable" class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                  <tr>
                    <th class="px-6 py-3 text-left text-xs font-semibold text-gray-600 uppercase">Tanggal</th>
                    <th class="px-6 py-3 text-left text-xs font-semibold text-gray-600 uppercase">Total Revenue</th>
                  </tr>
                </thead>
                <tbody id="salesTableBody"></tbody>
              </table>
            </div>
          </div>

          <!-- Kategori -->
          <div class="chart-container card-hover p-6 rounded-xl shadow-sm">
            <h2 class="text-xl font-bold text-gray-800 mb-4">Menu Terlaris (30 Hari Terakhir)</h2>
            <div class="overflow-x-auto custom-scrollbar">
              <table id="categoriesTable" class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                  <tr>
                    <th class="px-6 py-3 text-left text-xs font-semibold text-gray-600 uppercase">Nama Menu</th>
                    <th class="px-6 py-3 text-left text-xs font-semibold text-gray-600 uppercase">Total Terjual</th>
                  </tr>
                </thead>
                <tbody id="categoriesTableBody"></tbody>
              </table>
            </div>
          </div>
        </div>

        <!-- Pesanan Terbaru -->
        <section class="chart-container card-hover p-6 rounded-xl shadow-sm mb-8">
          <div class="flex items-center justify-between mb-6">
            <h2 class="text-xl font-bold text-gray-800">Pesanan Terbaru</h2>
          </div>
          <div class="overflow-x-auto custom-scrollbar">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase">ID Pesanan</th>
                  <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase">Tanggal</th>
                  <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase">Status</th>
                  <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase">Total</th>
                  <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase">Item</th>
                </tr>
              </thead>
              <tbody id="recentActivities" class="bg-white divide-y divide-gray-100"></tbody>
            </table>
          </div>
        </section>
      </main>
    </div>

    <!-- Script -->
    <script>
      function formatStatus(status) {
        const s = (status || '').toLowerCase();
        const map = {
          'pending': '<span class="status-badge status-pending">Pending</span>',
          'processing': '<span class="status-badge status-processing">Diproses</span>',
          'completed': '<span class="status-badge status-completed">Selesai</span>',
          'selesai': '<span class="status-badge status-completed">Selesai</span>',
          'cancelled': '<span class="status-badge status-cancelled">Dibatalkan</span>'
        };
        return map[s] || `<span class="status-badge status-pending">${status}</span>`;
      }

      async function fetchSummary() {
        const res = await fetch('/admin/api/dashboard/summary');
        const json = await res.json();
        if (json.success) {
          document.getElementById('revenueToday').innerText = 'Rp ' + Number(json.data.revenueToday).toLocaleString('id-ID');
          document.getElementById('totalPesanan').innerText = json.data.totalPesanan.toLocaleString('id-ID');
          document.getElementById('menuAktif').innerText = json.data.menuAktif.toLocaleString('id-ID');
          document.getElementById('customerAktif').innerText = json.data.customerAktif.toLocaleString('id-ID');
        }
      }

      async function fetchSales() {
        const res = await fetch('/admin/api/dashboard/sales?range=7');
        const json = await res.json();
        const tbody = document.getElementById('salesTableBody');
        tbody.innerHTML = '';
        const data = json.data?.series || [];
        if (data.length > 0) {
          data.forEach(r => {
            tbody.innerHTML += `
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 text-sm text-gray-700">${r.date}</td>
              <td class="px-6 py-4 text-sm font-semibold text-gray-900">Rp ${Number(r.revenue).toLocaleString('id-ID')}</td>
            </tr>`;
          });
        } else {
          tbody.innerHTML = '<tr><td colspan="2" class="px-6 py-4 text-center text-gray-500 text-sm">Belum ada data.</td></tr>';
        }
      }

      async function fetchTopMenus() {
        const res = await fetch('/admin/api/dashboard/top-menus?days=30');
        const json = await res.json();
        const tbody = document.getElementById('categoriesTableBody');
        tbody.innerHTML = '';
        const data = json.data || [];
        if (data.length > 0) {
          data.forEach(m => {
            tbody.innerHTML += `
        <tr class="hover:bg-gray-50">
          <td class="px-6 py-4 text-sm text-gray-700">${m.nama_menu}</td>
          <td class="px-6 py-4 text-sm font-semibold text-gray-900">${m.total_terjual}</td>
        </tr>`;
          });
        } else {
          tbody.innerHTML = '<tr><td colspan="2" class="px-6 py-4 text-center text-gray-500 text-sm">Belum ada data.</td></tr>';
        }
      }


      async function fetchRecentActivities() {
        const res = await fetch('/admin/api/dashboard/recent-activities?limit=6');
        const json = await res.json();
        const tbody = document.getElementById('recentActivities');
        tbody.innerHTML = '';
        if (json.success) {
          json.data.forEach(r => {
            tbody.innerHTML += `
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 text-sm font-medium text-gray-900">#${r.id_pesanan}</td>
              <td class="px-6 py-4 text-sm text-gray-600">${r.tanggal}</td>
              <td class="px-6 py-4 text-sm">${formatStatus(r.status)}</td>
              <td class="px-6 py-4 text-sm font-semibold text-gray-900">Rp ${Number(r.total_amount).toLocaleString('id-ID')}</td>
              <td class="px-6 py-4 text-sm text-gray-600">${r.items || '-'}</td>
            </tr>`;
          });
        }
      }

      document.addEventListener('DOMContentLoaded', () => {
        fetchSummary();
        fetchSales();
        fetchTopMenus(); // ✅ ubah ini
        fetchRecentActivities();
      });
    </script>
</body>

</html>